{
  "id": "4ef11f02",
  "title": "Implement local repo pruning (remove repos with no upstream)",
  "tags": [],
  "status": "done",
  "created_at": "2026-04-30T10:04:22.017Z",
  "parent_id": "99604cf4"
}

- After matching local repos to upstream repos, identify local repos with NO upstream match
- These are candidates for removal (upstream was deleted)
- Propose removal: interactive y/n for each, or print in --dry-run
- Actual removal: `fs::remove_dir_all` or `rm -rf`
- Add `--prune` flag to `sync` subcommand to enable this behavior (off by default, since it's destructive)

This task covers the detection + execution of local repo pruning.
