{
  "id": "597d2cb5",
  "title": "Implement action execution (clone, move, update)",
  "tags": [],
  "status": "done",
  "created_at": "2026-04-30T10:02:28.487Z",
  "parent_id": "99604cf4"
}

- Update action: `git stash -u`, then `git pull --rebase`. On failure, skip with warning.
- Move action: if interactive mode, prompt y/n for each move. If --dry-run, just print. Actual move = `fs::rename` (or `mv`).
- Clone action: `git clone <url> <expected_path>`.
- Execute in parallel with rayon (with concurrency limit like current code).
- Progress bar via indicatif.
- Summary at the end.
